# Normalise les fins de ligne : LF dans le repo, natif au checkout
* text=auto

# Forcer CRLF pour les scripts Windows
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Forcer LF pour les scripts Unix (futur Docker/deploy)
*.sh text eol=lf

# Binaires
*.png binary
*.jpg binary
*.ico binary
*.db binary
